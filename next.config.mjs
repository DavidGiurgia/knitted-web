/** @type {import('next').NextConfig} */
const nextConfig = {
    // Alte configurări
    images: {
      domains: ['i.ibb.co'],  // Adaugă aici domeniile permise
    },
  };

export default nextConfig;
